# NotionLink
(SOURCE-AVAILABLE)vA Windows tray utility that links local files/folders to Notion pages. It runs a local server to create clickable http://localhost links, letting you open files directly from Notion. Features automatic new file syncing (folder watching) and a GUI for managing mappings.
